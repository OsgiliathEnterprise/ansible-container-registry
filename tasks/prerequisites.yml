---

- name: Prerequisite | install prerequisite - pip
  ansible.builtin.package:
    name: python-pip
    state: present
  become: true

- name: Prerequisite | install prerequisite - docker
  ansible.builtin.pip:
    name: ["docker", "passlib"]
    state: present
  become: true
